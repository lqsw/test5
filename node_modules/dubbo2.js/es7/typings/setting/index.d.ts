import { IDubboSetting, TPredictFunction } from './types';
/**
 * Matcher
 * 多么想要一个ReasonML的match-pattern 😆
 */
export declare class Setting {
    private readonly _rules;
    private _cache;
    /**
     * 匹配规则
     * 规则的有限级别 string > string[] > fn > regexp
     * @param arg
     * @param dubboSetting
     */
    match(arg: string | Array<string> | RegExp | TPredictFunction, dubboSetting?: IDubboSetting): this;
    getDubboSetting(dubboInterface: string): IDubboSetting;
}
declare const _default: Setting;
export default _default;
